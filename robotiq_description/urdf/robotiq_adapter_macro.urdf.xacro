<?xml version="1.0" encoding="utf-8"?>
<robot xmlns:xacro="http://ros.org/wiki/xacro" name="robotiq_adapter">
  <xacro:macro name="robotiq_adapter" params="
  prefix
  parent
  child
  *origin">

    <joint name="${prefix}robotiq_joint" type="fixed">
      <!-- The parent link must be read from the robot model it is attached to. -->
      <parent link="${parent}"/>
      <child link="${prefix}robotiq_link"/>
      <xacro:insert_block name="origin"/>
    </joint>

    <link name="${prefix}robotiq_link">
        <visual>
          <origin xyz="0 0 0" rpy="0 0 0" />
          <geometry>
            <mesh filename="package://robotiq_description/meshes/visual/2f_85/robotiq_adapter.dae"/>
          </geometry>
        </visual>
        <collision>
          <origin xyz="0 0 0" rpy="0 0 0"/>
          <geometry>
            <mesh filename="package://robotiq_description/meshes/collision/2f_85/robotiq_adapter.stl"/>
          </geometry>
        </collision>
        <inertial>
          <mass value="0.01" />
          <origin xyz="0 0 0" />
          <inertia ixx="0.000044" ixy="0.0" ixz="0.0" iyy="0.000027" iyz="0.0" izz="0.000027" />
        </inertial>
    </link>

    <joint name="${prefix}gripper_side_joint" type="fixed">
      <parent link="${prefix}robotiq_link"/>
      <child link="${child}"/>
      <!-- <origin xyz="0 0 0.011" rpy="0 ${-pi/2} ${pi/2}"/> -->
      <origin xyz="0 0 0.011" rpy="0 0 0"/>
    </joint>
    <link name="${child}"/>

  </xacro:macro>
</robot>
